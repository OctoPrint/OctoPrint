 <div class="control-group" title="">
    <label class="control-label" for="settings-defaultWebcam">{{ _('Default Webcam') }}</label>
    <div class="controls">
        <select id="settings-defaultWebcam" data-bind="options: webcam_webcams,
                                optionsText: function(key) { return key.displayName },
                                optionsValue: function(key) { return key.name },
                                value: webcam_defaultWebcam">
        </select>

        <span class="help-inline">{{ _('Selects the default webcam. The default webcam will be used for timelapses and can also provide a MJPEG video stream for old clients that do not make use of the webcam plugin system introduced with OctoPrint 1.9') }}</span>

        <!-- ko foreach: webcam_webcams -->
            <!-- ko if: name == $parent.webcam_defaultWebcam() -->
                <table class="help-inline" style="padding-top: 10px">
                    <tr>
                        <td><b>{{ _('Snpashot URL')}}&nbsp;</b></td>
                        <td data-bind="text: snapshot || 'Not provided'"></td>
                    </tr>
                    <tr>
                        <td><b>{{ _('MJPEG stream')}}&nbsp;</b></td>
                        <td data-bind="text: (compat != null ? compat.stream : null) || 'Not provided'"></td>
                    </tr>
                </table>
            <!-- /ko -->
        <!-- /ko -->
    </div>
</div>
